"""LLM-4: guardrails — PII redaction, citation verification, topical scope,
and numeric consistency. Schema validation (LLM-4.2) lives in
`llm/structured_output.py` since it's inseparable from the generation call.
"""
